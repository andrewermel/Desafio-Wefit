version: '3.8'

services:
  mysqldb:
    platform: linux/x86_64
    image: mysql:5.6
    restart: unless-stopped
    env_file: ./.env
    environment:
      - MYSQL_ROOT_PASSWORD=senha_root_123
      - MYSQL_DATABASE=wefit
    ports:
      - 3306:3306
    volumes:
      - ./data/:/var/lib/mysql
